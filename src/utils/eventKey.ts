// KeyboardEvent.key values, kept as a single source so call sites and the
// on-screen Keypad agree on the same strings.
export const eventKey = {
  Escape: 'Escape',
  Enter: 'Enter',
  Space: ' ',
  ArrowLeft: 'ArrowLeft',
  ArrowUp: 'ArrowUp',
  ArrowRight: 'ArrowRight',
  ArrowDown: 'ArrowDown',
  Tab: 'Tab',
  Backspace: 'Backspace'
} as const;
